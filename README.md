# Docker

Script de docker com php 7.3 e 8.0, mysql, redis e nginx

# Primeiro comando

docker-compose up -d

# Parar container

docker-compose down

# Entrar no container

docker-compose exec <nome-container> sh
